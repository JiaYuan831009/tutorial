class ProductController {
  getProduct = (req, res) => {
    // 拿到 requset
    // 演算法
    if (!name || !phone) {
      res.status(400).json({ message: '輸入的內容有誤' })
      return;
    }
    // store into database
    // name phone
    // name
    // 回應
  }
}

export default new ProductController();